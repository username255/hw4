package game;

import org.junit.Assert;
import org.junit.Test;

public class RestaurantTests {
/*
	void recordSatisfaction(Restaurant restaurant, int times, String matter) {
		for (int i = 0; i < times; i++) {
			if (matter.startsWith("food"))
				restaurant.recordClientSatisfactionOnFood();
			else if (matter.startsWith("beverage"))
				restaurant.recordClientSatisfactionOnBeverage();
			else
				restaurant.recordClientSatisfactionOnService();				
		}
	}

	void recordDissatisfaction(Restaurant restaurant, int times, String matter) {
		for (int i = 0; i < times; i++) {
			if (matter.startsWith("food"))
				restaurant.recordClientDissatisfactionOnFood();
			else if (matter.startsWith("beverage"))
				restaurant.recordClientDissatisfactionOnBeverage();
			else
				restaurant.recordClientDissatisfactionOnService();				
		}
	}

	@Test
	public void test1() {
		Restaurant restaurant = null;
		restaurant = new Restaurant();
		restaurant.setReputation(20);
		recordSatisfaction(restaurant, 8, "food");
		recordSatisfaction(restaurant, 5, "beverages");
		recordSatisfaction(restaurant, 7, "service");
		recordDissatisfaction(restaurant, 2, "food");
		recordDissatisfaction(restaurant, 5, "beverages");
		recordDissatisfaction(restaurant, 3, "service");
		restaurant.updateReputation();
		Assert.assertEquals(30, restaurant.getReputation());
	}

	@Test
	public void test2() {
		Restaurant restaurant = null;
		restaurant = new Restaurant();
		restaurant.setReputation(10);
		recordSatisfaction(restaurant, 4, "food");
		recordSatisfaction(restaurant, 2, "beverages");
		recordSatisfaction(restaurant, 2, "service");
		recordDissatisfaction(restaurant, 2, "beverages");
		recordDissatisfaction(restaurant, 2, "service");
		restaurant.updateReputation();
		Assert.assertEquals(14, restaurant.getReputation());
	}*/

}
